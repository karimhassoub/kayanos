name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'yarn'
        
    - name: Install dependencies
      run: yarn install
      
    - name: Lint code
      run: yarn lint
      
    - name: Run tests
      run: yarn test:run
      
    - name: Run tests with coverage
      run: yarn test:coverage
      
    - name: Verify test coverage
      run: |
        if [ -d "coverage" ]; then
          echo "✅ Tests completed successfully with coverage"
          echo "Coverage report generated in coverage/ directory"
        else
          echo "❌ Test coverage report not generated"
          exit 1
        fi
